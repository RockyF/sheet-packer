{
  "name": "sheet-packer",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "dev": "rollup -c -w",
    "build": "rollup -c",
    "build:prod": "rollup -c --environment BUILD:production"
  },
  "dependencies": {
    "canvas": "^2.6.1",
    "fs-extra": "^9.0.0",
    "rects-bin-pack": "^0.1.3"
  },
  "devDependencies": {
    "rollup-plugin-progress": "^1.1.2",
    "rollup-plugin-uglify": "^6.0.4"
  }
}
